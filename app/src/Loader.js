import React, { Component } from 'react';

/*
 * Simple loader.
 */
class Loader extends Component {
    constructor(props) {
        super(props);
    }
    render() {
        return (
            <div>Loading...</div>
        );
    }
};

export default Loader;
